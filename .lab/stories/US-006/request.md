# Original Request

> This is the user's durable input. Roles may scope it, but must not silently rewrite it.

## Prompt

Posterize Catfish Lab into an approachable, memorable terminal identity and replace automatic first-start tutorial playback with an optional invitation that includes a persistent Don't show again checkbox.
