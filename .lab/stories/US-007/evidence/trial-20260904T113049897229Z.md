# Completion Trial

Story: US-007

## Acceptance Criterion 1 — evidence-first Advance gate

Verdict: PASS

Evidence: `tests/test_lab_tui.py` opens a trial-stage case with `a`, enters the assembled source view
with `e`, scrolls it with `j`, returns with Esc, and accepts with `y`. The test observes the durable
`done_approved` transition. `AdvanceScreen` initially focuses Review Evidence and retains Tab, Enter,
and mouse alternatives.

## Acceptance Criterion 2 — readable review controls

Verdict: PASS

Evidence: every role and evidence reading screen uses `ReviewScroll`, which adds `j`/`k` while
retaining arrows, Home/End, Page Up/Down, and the scrollbar. Help and handbook copy expose the same
route.

## Acceptance Criterion 3 — paced tutorial entrance

Verdict: PASS

Evidence: the First Cast enters a native orientation explaining its synthetic boundary, sequence,
and controls. The producer waits for `b`; default inter-step pacing increased from 0.65 to 1.5
seconds. Space pauses or resumes. Separate tests prove no start before orientation and no next event
while paused, including a pause requested during the timer.

## Acceptance Criterion 4 — visible failures

Verdict: PASS

Evidence: `_start_model_action` persists unexpected provider exceptions as `work_delayed` events.
The TUI reconstructs a selected story's latest DELAYED reason from the ledger whenever its worker is
idle. Tests exercise a token-quota exception and confirm the exact reason remains in the status line
after a new application instance starts.

## Acceptance Criterion 5 — prior keyboard behavior remains intact

Verdict: PASS

Evidence: all eight role keys work, digits remain typeable in prompts, and arrow/j/k story selection
survives polling and newly arriving stories.

## Verification

- `pytest -q` — 63 passed.
- `ruff check .` — passed.
- `lab doctor` — passed.
- `mkdocs build --strict` — passed.
- Offline source distribution and wheel build — passed; active TUI/tutorial assets present and
  archive/live state absent.

## Red Team Findings

The review reproduced the missing evidence route, reading-key gap, abrupt tutorial, and ephemeral
delay display. No deterministic defect remains after remediation. Large real-world evidence bundles
and subjective tutorial pacing remain appropriate human observations rather than invented proof.

## Overall Verdict

READY

## Missing Evidence

- Human visual and pacing review in the presentation terminal.
- Live Docker, Windows, macOS, Claude, and Ollama runs.
- GitHub community-profile result after repository creation.
